apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: application3-image-policy
  namespace: default
spec:
  imageRepositoryRef:
    name: application3-repository  # Reference the ImageRepository
  policy:
    semver:
      range: ">=1.0.x"  # Correct format for semver range
